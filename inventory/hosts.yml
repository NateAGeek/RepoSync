---
all:
  children:
    # Local testing
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
    
    # Production servers
    servers:
      hosts:
        # Add your VPS servers here
        # Example:
        # my-vps:
        #   ansible_host: 192.168.1.10
        #   ansible_user: ubuntu
        #   tailscale_ip: 100.64.0.1  # Set after Tailscale connects
      vars:
        ansible_python_interpreter: /usr/bin/python3
